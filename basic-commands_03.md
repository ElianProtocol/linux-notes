# 📘 Конспект: Текстовые редакторы (nano и vim)

> **Редактирование файлов в консоли Linux: базовый nano для повседневной работы и минимальные навыки выживания в vim.**

## 📋 Содержание
 
- [1. Зачем нужен текстовый редактор в терминале](#1-зачем-нужен-текстовый-редактор-в-терминале)
- [2. nano — основной редактор](#2-nano--основной-редактор)
  - [Интерфейс и нотация](#интерфейс-и-нотация)
  - [Основные команды nano](#основные-команды-nano)
  - [Полезные приёмы](#полезные-приёмы-nano)
- [3. vim — минимальный обзор](#3-vim--минимальный-обзор)
  - [Два режима (главное отличие от nano)](#два-режима-главное-отличие-от-nano)
  - [Минимальный набор команд](#минимальный-набор-команд-vim)
  - [Дополнительные команды (нормальный режим)](#дополнительные-команды-нормальный-режим)
  - [Смена редактора по умолчанию](#смена-редактора-по-умолчанию)
- [4. Практические приёмы](#4-практические-приёмы)
  - [Работа с файлами проекта](#работа-с-файлами-проекта)
  - [Типичный сценарий в nano](#типичный-сценарий-в-nano)
  - [Типичный сценарий в vim](#типичный-сценарий-в-vim)
- [5. Справочник команд](#5-справочник-команд)
  - [nano (команды)](#nano-команды)
  - [vim (команды)](#vim-команды)
- [6. Итоги](#6-итоги)

---

## 1. Зачем нужен текстовый редактор в терминале

На серверах нет графического интерфейса, поэтому для редактирования файлов (конфигов, скриптов) нужны **консольные редакторы**.

Два основных:

| Редактор | Характеристика |
|----------|----------------|
| **nano** | Простой, интуитивный, подходит для новичков |
| **vim** | Мощный, но со сложным управлением (важно знать, как выйти) |

---

## 2. nano — основной редактор

### Интерфейс и нотация

- `^X` означает **Ctrl+X**
- `M-U` означает **Alt+U** (Meta)

**Экран разделён на три зоны:**

| Зона | Содержание |
|------|------------|
| Верхняя строка | Имя файла и статус |
| Основная область | Содержимое файла |
| Нижние две строки | Подсказки по командам |

### Основные команды nano

| Команда | Действие |
|---------|----------|
| `^O` | Сохранить файл |
| `^X` | Выйти из nano |
| `^K` | Вырезать (удалить) строку |
| `^U` | Вставить вырезанную строку |
| `^W` | Найти текст |
| `^\` | Найти и заменить |
| `^_` | Перейти к строке по номеру |
| `Alt+U` | Отменить действие (undo) |
| `Alt+N` | Вкл/выкл номера строк |

> **Стандартный сценарий:** `^X` → `Y` → `Enter` — сохранить и выйти.

### Полезные приёмы nano

```bash
# Открыть файл с номерами строк
nano -l notes.txt

# Открыть файл на определённой строке
nano +42 config.txt
```

**Включение подсветки синтаксиса** — добавьте в `~/.nanorc`:

```
include "/usr/share/nano/*.nanorc"
```

---

## 3. vim — минимальный обзор

### Два режима (главное отличие от nano)

```
Нормальный режим (Normal)  ←── vim запускается здесь
       │                        (клавиши = команды)
       │ i — войти в режим вставки
       ▼
Режим вставки (Insert)     ←── здесь можно печатать текст
       │
       │ Esc — вернуться в нормальный режим
       ▼
Нормальный режим
```

> **Правило:** если что-то пошло не так — нажмите **`Esc`**.

### Минимальный набор команд vim

| Действие | Команда |
|----------|---------|
| Войти в режим вставки | `i` |
| Вернуться в нормальный режим | `Esc` |
| Сохранить | `:w` + `Enter` |
| Выйти | `:q` + `Enter` |
| Сохранить и выйти | `:wq` + `Enter` |
| Выйти без сохранения | `:q!` + `Enter` |

> **Спасательная команда:** если vim открылся и вы не знаете, что делать —  
> `Esc` → `:q!` → `Enter` (выйти без сохранения).

### Дополнительные команды (нормальный режим)

| Команда | Действие |
|---------|----------|
| `x` | Удалить символ под курсором |
| `dd` | Удалить строку |
| `u` | Отменить действие |

### Смена редактора по умолчанию

Если программы (`git`, `crontab`) открывают vim, а вы хотите nano:

```bash
export EDITOR=nano                    # временно, на сессию
echo 'export EDITOR=nano' >> ~/.bashrc # навсегда
```

---

## 4. Практические приёмы

### Работа с файлами проекта

```bash
# Открыть конфиг в nano
nano wordpress-project/config/nginx.conf

# Открыть HTML-файл в vim
vim wordpress-project/site/index.html
```

### Типичный сценарий в nano

| Шаг | Действие |
|-----|----------|
| 1 | Открыть файл: `nano file.conf` |
| 2 | Внести изменения (печатать текст) |
| 3 | Найти нужное место: `^W` (поиск) |
| 4 | Сохранить: `^O` → `Enter` |
| 5 | Выйти: `^X` |

### Типичный сценарий в vim

| Шаг | Действие |
|-----|----------|
| 1 | Открыть файл: `vim file.conf` |
| 2 | Войти в режим вставки: `i` |
| 3 | Внести изменения |
| 4 | Вернуться в нормальный режим: `Esc` |
| 5 | Сохранить и выйти: `:wq` + `Enter` |

---

## 5. Справочник команд

### nano (команды)

| Что нужно | Что нажать |
|-----------|------------|
| Сохранить | `Ctrl+O`, `Enter` |
| Выйти | `Ctrl+X` |
| Найти | `Ctrl+W` |
| Вырезать строку | `Ctrl+K` |
| Вставить | `Ctrl+U` |
| Отменить | `Alt+U` |

### vim (команды)

| Что нужно | Что нажать |
|-----------|------------|
| Начать печатать | `i` |
| Закончить печатать | `Esc` |
| Сохранить | `:w` + `Enter` |
| Выйти | `:q` + `Enter` |
| Сохранить и выйти | `:wq` + `Enter` |
| Выйти без сохранения | `:q!` + `Enter` |

---

## 6. Итоги

**Что нужно запомнить:**

| № | Что узнали | Ключевое |
|---|------------|----------|
| 1 | **nano** | Основной редактор для повседневной работы |
| 2 | **nano — сохранить** | `^O` (Ctrl+O) |
| 3 | **nano — выйти** | `^X` (Ctrl+X) |
| 4 | **nano — найти** | `^W` (Ctrl+W) |
| 5 | **vim — режимы** | Нормальный (команды) и режим вставки (печать) |
| 6 | **vim — начать печатать** | `i` |
| 7 | **vim — выйти без сохранения** | `Esc` → `:q!` → `Enter` |
| 8 | **vim — сохранить и выйти** | `Esc` → `:wq` → `Enter` |

**📌 Ключевое правило администратора:** Если vim открылся неожиданно — не паникуйте. Нажмите `Esc`, затем `:q!` и `Enter`. Это гарантированно вернёт вас в терминал без сохранения изменений.

**📌 Типичная ошибка:** Попытка печатать в vim, не переключившись в режим вставки

```bash
# ❌ В нормальном режиме клавиши работают как команды
# Вы нажимаете буквы, а vim их не печатает

# ✅ Нажмите i, чтобы войти в режим вставки
# Затем печатайте текст
```

**📌 Полезная команда для настройки nano по умолчанию:**

```bash
# Сделать nano редактором по умолчанию для всех программ
echo 'export EDITOR=nano' >> ~/.bashrc
echo 'export VISUAL=nano' >> ~/.bashrc
source ~/.bashrc
```

---

**📌 Готово.** Теперь вы умеете редактировать файлы в консоли: уверенно работаете в nano и знаете, как выйти из vim в любой ситуации.
